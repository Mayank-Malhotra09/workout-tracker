import React from 'react'
import ReactDOM from 'react-dom/client'
import WorkoutTracker from './lift_log.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WorkoutTracker />
  </React.StrictMode>,
)
